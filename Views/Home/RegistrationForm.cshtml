@model RegistrationModel

<h1 class=" row col-12 justify-content-center">Регистрация</h1>

<form asp-controller="Home" asp-action="Registration">
    <div class="form-inline row col-12 m-1">
        <label class="control-label col-3">Имя</label>
        <input class="form-control col-6" name="firstName" type="search" placeholder="Имя">
    </div>
    
    <div class="form-inline row col-12 m-1">
        <label class="control-label col-3">Фамилия</label>
        <input class="form-control col-6" name="secondName" type="search" placeholder="Фамилия">
    </div>
    
    <div class="form-inline row col-12 m-1">
        <label class="control-label col-3">Email</label>
        <input class="form-control col-6" name="email" type="search" placeholder="e-mail">
    </div>
    
    <div class="form-inline row col-12 m-1">
        <label class="control-label col-3">Пароль</label>
        <input class="form-control col-6" name="password" type="search" placeholder="Пароль">
    </div>
    
    <div class="row col-12 justify-content-center m-1">
        <div class="dropdown col-6">
            <button class="btn btn-secondary dropdown-toggle col-12" type="button" id="dropdownMenu1" data-toggle="dropdown" aria-haspopup="true" aria-expanded="false">
                Мои компетенции
            </button>
            <div class="dropdown-menu col-12" aria-labelledby="dropdownMenu1">
                @foreach (var c in Model.CurrentCompetencies)
                {
                    <div class="dropdown-item form-check">
                        <input class="form-check-input" value="@c" type="checkbox" name="current" id="@c">
                        <label class="form-check-label" for="@c">@c</label>
                    </div>
                }
            </div>
        </div>
    </div>
    
    <div class="row col-12 justify-content-center m-1">
        <div class="dropdown col-6">
            <button class="btn btn-secondary dropdown-toggle col-12" type="button" id="dropdownMenu2" data-toggle="dropdown" aria-haspopup="true" aria-expanded="false">
                Желаемые компетенции
            </button>
            <div class="dropdown-menu col-12" aria-labelledby="dropdownMenu2">
                @foreach (var c in Model.DesiredCompetencies)
                {
                    <div class="dropdown-item form-check">
                        <input class="form-check-input" value="@c" type="checkbox" name="desired" id="@c">
                        <label class="form-check-label" for="@c">@c</label>
                    </div>
                }
            </div>
        </div>
    </div>
    
    <!--<div class="row col-12 justify-content-center m-1">
        <div class="dropdown col-6">
            <button class="btn btn-secondary dropdown-toggle col-12" type="button" id="dropdownMenu3" data-toggle="dropdown" aria-haspopup="true" aria-expanded="false">
                Интересующие темы
            </button>
            <div class="dropdown-menu col-12" aria-labelledby="dropdownMenu3">
                @foreach (var theme in Model.Themes)
                {
                    <div class="dropdown-item form-check">
                        <input class="form-check-input" value="@theme" type="checkbox" name="themes" id="@theme">
                        <label class="form-check-label" for="@theme">@theme</label>
                    </div>
                }
            </div>
        </div>
    </div>-->

    <div class="row col-12 justify-content-center">
        <button class="btn btn-outline-success " type="submit">Зарегистрироваться</button>
    </div>
</form>